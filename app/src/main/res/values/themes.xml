<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- AppCompat base theme is required by AppCompatActivity (used for per-app language).
         Compose (PantryHubTheme) drives the actual colors; this only styles the window. -->
    <style name="Theme.PantryHub" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>